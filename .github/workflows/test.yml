name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

# Only the newest push to a branch matters. Without this, every push leaves its
# predecessor's jobs queued, and on a small self-hosted pool those stale runs
# are what everything else waits behind.
concurrency:
  group: test-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: [self-hosted, homelab]
    # The suite takes about two minutes. This is not a limit on the tests, it is
    # a limit on a dead runner: when an ARC pod dies mid-job the job stays
    # "in_progress" forever, holds its slot, and starves the queue behind it in
    # total silence. One such job blocked a release for 95 minutes.
    timeout-minutes: 10
    container:
      image: python:${{ matrix.python-version }}-slim
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install uv
        run: pip install uv

      - name: Install dependencies
        run: uv pip install --system -e ".[dev]"

      - name: Lint
        run: |
          ruff check src/ tests/
          ruff format --check src/ tests/

      - name: Test
        run: pytest tests/ -v

      # The core has to keep working on a machine that cannot afford rich —
      # a pfSense or OpenWrt box wants the tunnel, not a rendering library.
      # Asserting it in a venv that genuinely lacks rich is the only way to
      # know; with rich installed, every fallback path is dead code.
      - name: Test without the pretty extra
        run: |
          python -m venv /tmp/norich
          /tmp/norich/bin/pip install --quiet . pytest pytest-asyncio
          if /tmp/norich/bin/python -c "import rich" 2>/dev/null; then
            echo "::error::rich was installed by a bare 'pip install .' — it must stay an extra"
            exit 1
          fi
          /tmp/norich/bin/python -m pytest tests/ -q

      - name: Test the plugins
        run: |
          # POSIX sh: these steps run under `sh -e`, where `shopt` does not
          # exist and cost this job an exit-127 before it ran anything. An
          # unmatched glob stays literal instead, which the -d test catches.
          set -eu
          for plugin in plugins/*/; do
            [ -d "$plugin" ] || continue
            [ -f "${plugin}pyproject.toml" ] || continue
            echo "::group::$(basename "$plugin")"
            uv pip install --system -e "${plugin}[dev]"
            (cd "$plugin" && ruff check src/ tests/ && ruff format --check src/ tests/)
            (cd "$plugin" && pytest tests/ -q)
            echo "::endgroup::"
          done

      - name: Verify imports
        run: |
          python -c "from hle_client.cli import main; print('CLI import OK')"
          python -c "from hle_common.protocol import MessageType; print('Protocol import OK')"
          python -c "from hle_common.models import TunnelRegistration; print('Models import OK')"
