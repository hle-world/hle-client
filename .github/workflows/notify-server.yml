name: Notify Server Repo

on:
  push:
    branches: [main]
    paths: ['src/hle_common/**']

permissions:
  contents: read

jobs:
  notify:
    runs-on: arc-runner-hle-client
    container:
      image: node:20-slim
    steps:
      - uses: peter-evans/repository-dispatch@11446b25a5fd252975d4bdf43d8989a5ac4f16c5
        with:
          token: ${{ secrets.HLE_PAT }}
          repository: jspanos/hle
          event-type: common-updated
