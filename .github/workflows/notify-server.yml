name: Notify Server Repo

on:
  push:
    branches: [main]
    paths: ['src/hle_common/**']

permissions:
  contents: read

jobs:
  notify:
    runs-on: arc-runner-hle-client
    container:
      image: node:20-slim
    steps:
      - uses: peter-evans/repository-dispatch@ce061ce2ce7e214fb921237d38dc6779a5f383b3
        with:
          token: ${{ secrets.HLE_PAT }}
          repository: jspanos/hle
          event-type: common-updated
